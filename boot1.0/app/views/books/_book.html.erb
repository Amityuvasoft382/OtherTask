<% @books.each do |book|%>

<tr>
<td><%= book.name%></td>
<td><%= book.gen %></td>
<td><%= link_to "Show", book %></td>

<td><%= link_to "Edit", edit_book_path(book) %></td>
<td><%= link_to "delete", book, method: :delete,remote:true,data: { confirm: 'Are you sure?' }%></td>
</tr>

<%end%>