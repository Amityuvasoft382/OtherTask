# This file should contain all the record creation needed to seed the database with its default values.
# The data can then be loaded with the bin/rails db:seed command (or created alongside the database with db:setup).
#
# Examples:
#
#   movies = Movie.create([{ name: 'Star Wars' }, { name: 'Lord of the Rings' }])
#   Character.create(name: 'Luke', movie: movies.first)
 require 'faker'
# 5.times do
# 	for i in 1..4 do
# 		@user= User.find(i)
# 		@user.posts.create(title:Faker::Quote.singular_siegler,detail:Faker::Quote.matz)
# 	end
# end

 # 1.times do
 # 	for i in 1..Post.all.count do
 # 	@p = Post.find(i)
 # 	for j in 1..4 do
 # 		@p.comments.create(user_id:User.find(j).id,user:User.find(j).username,com:Faker::Quote.jack_handey)
 # 	end 
 # 	end
 # end